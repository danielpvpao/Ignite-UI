{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build"
  },
  "devDependencies": {
    "turbo": "^2.3.3"
  },
  "packageManager": "npm@7.0.0",
  "esbuild": "0.24.0",
  "dependencies": {
    "git-up": "^8.0.0",
    "git-url-parse": "^16.0.0"
  }
}
